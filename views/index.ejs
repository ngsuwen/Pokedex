<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Pokedex</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <style>

        body { 
            padding-top: 70px; 
        }
        .image {
            position: relative;
        }
        .image img {
            width: 100%;
            height: 10vw;
            object-fit: contain;
        }
        .image:hover img {
            opacity: 0.2;
        }
        .image .hide {
            display: none;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        .image:hover .hide {
            display: flex;
            align-items: center;
            flex-direction: column;
        }
        button {
            width: 5rem;
        }

    </style>
</head>

<body>
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                  <a class="nav-link active" href="/">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/new">New</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="container d-flex flex-wrap">
            <%for (let i=0;i<data.length;i++){%>

                <div class="col-sm-2 d-flex flex-column align-items-stretch">
                    <div class="image">
                        <img class="p-3" alt="Image" src=<%=data[i].img%>>
                        <div class="hide">
                            <form action="/<%=i%>"><button class="btn btn-warning btn-sm">View</button></form>
                            <form action="/edit/<%=i%>"><button class="btn btn-success btn-sm">Edit</button></form>
                            <form action="/<%=i%>/?_method=DELETE" method="POST"><button class="btn btn-danger btn-sm">Delete</button></form>
                        </div>
                    </div>
                    <h5>
                        <%=data[i].name%>
                    </h5>
                    <p>#<%=data[i].id%>
                    </p>
                </div>

                <% } %>
        </div>
    </div>
</body>

</html>